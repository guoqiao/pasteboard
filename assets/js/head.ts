/**
 * Head assets bundle entry. The <head> bundle must not depend on jQuery,
 * so this only exists to mirror the old `head.coffee` entry point.
 * The actual content is the Modernizr and -prefix-free libraries that the
 * build script prepends.
 */